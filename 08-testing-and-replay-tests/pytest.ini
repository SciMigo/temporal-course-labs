[pytest]
# Tests are plain `async def`; conftest.py runs them with asyncio.run so no plugin is required
# (pytest-asyncio, if installed, is left in its default strict mode and does not interfere).
testpaths = tests
